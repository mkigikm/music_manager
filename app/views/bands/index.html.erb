<% content_for :title do %>
<h1>Bands</h1>
<% end %>

<ul>
  <% @bands.each do |band| %>
  <li><a href="<%= band_url(band) %>"><%= band.name %></a></li>
  <% end %>
</ul>

<p>
  <a href="<%= new_band_url %>">Add a Band</a>
</p>
